<!--
Autogenerated file: StatusEvent
2017-11-05 04:11:35
-->

<div id="StatusEvent-form-content">
    <form id="StatusEvent-main-form" method="<%= method %>" action="<%= action %>"  class="form">
            <div class="form-group">
                <label for="uuid">Uuid</label>
                <input type="text" name="uuid"  
                    id="statusevent_uuid"
                    placeholder="Uuid"
                    value="<%= record.uuid ? record.uuid : '' %>"
                    class="form-control"
                    />
            </div>
    
        <div class="form-group">
            <label for="id">Id</label>
            <input type="text" name="id"  
                id="statusevent_id"
                placeholder="Id"
                <% if(crudAction !== 'create'){ %>
                disabled="disabled"
                <% } %>
                value="<%= record.id ? record.id : '' %>"
                class="form-control"
                />
        </div>
    
            <div class="form-group">
                <label for="job">Job</label>
                <select id="statusevent_job"
                    name="job"
                    data-reluri="<%=  %>"
                    data-crudaction="<%= locals.crudAction %>"
                    data-uri="<%= urlFor(locals.crudAction, '/job'); %>"
                    data-rel="one-to-many"
                    
                    value="<%= record.job ? record.job : '' %>"

                    
                    class="js-select-autopopulate form-control"
                     >
                
                </select>
            </div>
    
            <div class="form-group">
                <label for="application">Application</label>
                <select id="statusevent_application"
                    name="application"
                    data-reluri="<%=  %>"
                    data-crudaction="<%= locals.crudAction %>"
                    data-uri="<%= urlFor(locals.crudAction, '/application'); %>"
                    data-rel="one-to-many"
                    
                    value="<%= record.application ? record.application : '' %>"

                    
                    class="js-select-autopopulate form-control"
                     >
                
                </select>
            </div>
    
            <div class="form-group">
                <label for="downtime">Downtime</label>
                <input type="number" name="downtime"  
                    id="statusevent_downtime"
                    placeholder="Downtime"
                    value="<%= record.downtime ? record.downtime : '' %>"
                    class="form-control"
                    />
            </div>
    
            <div class="form-group">
                <label for="label">Label</label>
                <input type="text" name="label"  
                    id="statusevent_label"
                    placeholder="Label"
                    value="<%= record.label ? record.label : '' %>"
                    class="form-control"
                    />
            </div>
    
            <div class="form-group">
                <label for="description">Description</label>
                <input type="text" name="description"  
                    id="statusevent_description"
                    placeholder="Description"
                    value="<%= record.description ? record.description : '' %>"
                    class="form-control"
                    />
            </div>
    

        <% if(record.id){ %>
            <input hidden="true" name="id" value="<%= record.id %>" required/>
        <% } %>

        <div class="pull-right">
        <% if(record.id){ %>
        <button
            type="button"
            value="Delete"
            class="btn btn-danger">
                Delete
        </button>            
        <% } %>
        <button
            type="submit"
            value="Save"
            class="btn btn-primary">
                Save
        </button>
        <!-- this should be a conditional -->
        <button
            type="reset"
            value="Cancel"
            class="btn btn-default">
                Cancel
        </button>
        </div>
    </form>
</div>
