<!--
Autogenerated file: Check
2017-11-05 04:11:35
-->

<div id="Check-form-content">
    <form id="Check-main-form" method="<%= method %>" action="<%= action %>"  class="form">
            <div class="form-group">
                <label for="uuid">Uuid</label>
                <input type="text" name="uuid"  
                    id="check_uuid"
                    placeholder="Uuid"
                    value="<%= record.uuid ? record.uuid : '' %>"
                    class="form-control"
                    />
            </div>
    
        <div class="form-group">
            <label for="id">Id</label>
            <input type="text" name="id"  
                id="check_id"
                placeholder="Id"
                <% if(crudAction !== 'create'){ %>
                disabled="disabled"
                <% } %>
                value="<%= record.id ? record.id : '' %>"
                class="form-control"
                />
        </div>
    
            <div class="form-group">
                <label for="isUp">Is Up</label>
                <input type="" name="isUp"  
                    id="check_isUp"
                    placeholder="Is Up"
                    value="<%= record.isUp ? record.isUp : 'false' %>"
                    class="form-control"
                    />
            </div>
    
            <div class="form-group">
                <label for="isResponsive">Is Responsive</label>
                <input type="" name="isResponsive"  
                    id="check_isResponsive"
                    placeholder="Is Responsive"
                    value="<%= record.isResponsive ? record.isResponsive : 'false' %>"
                    class="form-control"
                    />
            </div>
    
            <div class="form-group">
                <label for="responseTime">Response Time</label>
                <input type="number" name="responseTime"  
                    id="check_responseTime"
                    placeholder="Response Time"
                    value="<%= record.responseTime ? record.responseTime : '' %>"
                    class="form-control"
                    />
            </div>
    
            <div class="form-group">
                <label for="requestTime">Request Time</label>
                <input type="number" name="requestTime"  
                    id="check_requestTime"
                    placeholder="Request Time"
                    value="<%= record.requestTime ? record.requestTime : '' %>"
                    class="form-control"
                    />
            </div>
    
            <div class="form-group">
                <label for="statusCode">Status Code</label>
                <input type="number" name="statusCode"  
                    id="check_statusCode"
                    placeholder="Status Code"
                    value="<%= record.statusCode ? record.statusCode : '' %>"
                    class="form-control"
                    />
            </div>
    
            <div class="form-group">
                <label for="job">Job</label>
                <select id="check_job"
                    name="job"
                    data-reluri="<%=  %>"
                    data-crudaction="<%= locals.crudAction %>"
                    data-uri="<%= urlFor(locals.crudAction, '/job'); %>"
                    data-rel="one-to-many"
                    
                    value="<%= record.job ? record.job : '' %>"

                    
                    class="js-select-autopopulate form-control"
                     >
                
                </select>
            </div>
    
            <div class="form-group">
                <label for="tags">Tags</label>
                <input type="text" name="tags"  
                    id="check_tags"
                    placeholder="Tags"
                    value="<%= record.tags ? record.tags : '' %>"
                    class="form-control"
                    />
            </div>
    
            <div class="form-group">
                <label for="error">Error</label>
                <input type="text" name="error"  
                    id="check_error"
                    placeholder="Error"
                    value="<%= record.error ? record.error : '' %>"
                    class="form-control"
                    />
            </div>
    
            <div class="form-group">
                <label for="endpoint">Endpoint</label>
                <input type="text" name="endpoint"  
                    id="check_endpoint"
                    placeholder="Endpoint"
                    value="<%= record.endpoint ? record.endpoint : '' %>"
                    class="form-control"
                    />
            </div>
    

        <% if(record.id){ %>
            <input hidden="true" name="id" value="<%= record.id %>" required/>
        <% } %>

        <div class="pull-right">
        <% if(record.id){ %>
        <button
            type="button"
            value="Delete"
            class="btn btn-danger">
                Delete
        </button>            
        <% } %>
        <button
            type="submit"
            value="Save"
            class="btn btn-primary">
                Save
        </button>
        <!-- this should be a conditional -->
        <button
            type="reset"
            value="Cancel"
            class="btn btn-default">
                Cancel
        </button>
        </div>
    </form>
</div>
